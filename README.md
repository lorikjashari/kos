# KoS

Browser FPS game built with TypeScript, Vite, Three.js, and Ammo.js.

## Develop

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```

## Deploy

Static Vite app — works on Vercel with default Vite settings (`build` → `dist`).
